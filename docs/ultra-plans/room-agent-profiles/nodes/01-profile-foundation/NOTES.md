# Notes

- Review highlighted `Restart_notify.parse_channel_from_key` as a specific parser regression risk. Keep this in the typed-key task acceptance criteria.
- Workspace lifecycle should preserve by default and only purge under explicit retention/force rules.
